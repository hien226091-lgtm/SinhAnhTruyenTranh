"""Top-level proxy package for running the FastAPI app from the repo root.

This package forwards imports to `api_base.app` so commands like
`uvicorn app.main:app` work when executed from the project root.
"""

__all__ = []
